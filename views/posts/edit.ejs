<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post - DOS Games Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="/stylesheets/partials.css">
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  
  <main>
    <h1>Edit Post</h1>

    <form action="/posts/<%= post._id %>?_method=PUT" method="POST">
      <div>
        <label for="title">Title:</label>
        <input type="text" name="title" id="title" value="<%= post.title %>" required>
      </div>

      <div>
        <label for="body">Post Body:</label>
        <textarea name="body" id="body" required><%= post.body %></textarea>
      </div>

      <div>
        <label for="coverImage">Cover Image URL:</label>
        <input type="text" name="coverImage" id="coverImage" value="<%= post.coverImage || '' %>" placeholder="https://example.com/image.jpg">
      </div>

      <div>
        <label for="gameFile">Game File URL (.jsdos):</label>
        <input type="text" name="gameFile" id="gameFile" value="<%= post.gameFile || '' %>" placeholder="https://example.com/game.jsdos">
      </div>

      <button type="submit">Update Post</button>
    </form>
    
    <a href="/posts/<%= post._id %>">Cancel</a>
  </main>

  <%- include('../partials/_footer.ejs') %>
</body>
</html>